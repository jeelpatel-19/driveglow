{
  "name": "driveglow-detailing",
  "version": "1.0.0",
  "description": "Premium Luxury Automotive Detailing Brand Website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "powershell -Command \"$pid3000 = (netstat -ano | Select-String ':3000 .*LISTENING').ToString().Trim().Split()[-1]; if ($pid3000) { taskkill /F /PID $pid3000 2>$null }\" & node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "pdfkit": "^0.19.1",
    "resend": "^6.17.2"
  }
}
